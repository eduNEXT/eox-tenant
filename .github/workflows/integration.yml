name: Integration Tests Quince, Palm

on: push

jobs:
  edx-platform-integration-test:
    runs-on: ubuntu-latest
    name: Test integration Felipe
    steps:
      - uses: luisfelipec95/test-integration@main
        with:
          tutor_version: '17.0.0'
          edunext_plugin: 'eox-tenant'
          edunext_plugin_folder: 'eox_tenant'
          plugin_branch_name: 'lfc/test-matrix'

